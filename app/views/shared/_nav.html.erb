<nav class="navbar navbar-light bg-light">

  <div>
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    <%= link_to 'Rewards', badges_path, class: 'navbar-text ml-5' %>
    <%= link_to 'Contact Us', contact_us_path, class: 'navbar-text ml-5' %>
    <% if user_signed_in? && current_user.is_admin? %>
      <%= link_to 'To Admin Panel', admin_tests_path, class: 'navbar-text ml-5' %>
    <% end %>
  </div>

  <span class="navbar-text">
    <% if user_signed_in? %>
        Welcome, <b><%= current_user.first_name %></b> Guru |
        <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
    <% else %>
        <%= link_to t('.sign_in'), new_session_path(resource_name) %> |
        <%= link_to t('.sign_up'), new_registration_path(resource_name) %>
    <% end %>
  </span>

</nav>